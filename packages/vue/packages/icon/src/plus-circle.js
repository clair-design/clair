import { makeIconComponent } from './utils/icon-wrapper'
import { PlusCircle } from '@clair/icons'
export const IconPlusCircle = /*@__PURE__*/ makeIconComponent({
  name: 'CIconPlusCircle',
  template: PlusCircle
})
