import { makeIconComponent } from './utils/icon-wrapper'
import { Camera } from '@clair/icons'
export const IconCamera = /*@__PURE__*/ makeIconComponent({
  name: 'CIconCamera',
  template: Camera
})
