import { makeIconComponent } from './utils/icon-wrapper'
import { Server } from '@clair/icons'
export const IconServer = /*@__PURE__*/ makeIconComponent({
  name: 'CIconServer',
  template: Server
})
