import { makeIconComponent } from './utils/icon-wrapper'
import { History } from '@clair/icons'
export const IconHistory = /*@__PURE__*/ makeIconComponent({
  name: 'CIconHistory',
  template: History
})
