import { makeIconComponent } from './utils/icon-wrapper'
import { Player } from '@clair/icons'
export const IconPlayer = /*@__PURE__*/ makeIconComponent({
  name: 'CIconPlayer',
  template: Player
})
