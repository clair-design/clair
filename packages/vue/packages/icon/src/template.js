import { makeIconComponent } from './utils/icon-wrapper'
import { Template } from '@clair/icons'
export const IconTemplate = /*@__PURE__*/ makeIconComponent({
  name: 'CIconTemplate',
  template: Template
})
