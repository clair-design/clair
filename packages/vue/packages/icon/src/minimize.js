import { makeIconComponent } from './utils/icon-wrapper'
import { Minimize } from '@clair/icons'
export const IconMinimize = /*@__PURE__*/ makeIconComponent({
  name: 'CIconMinimize',
  template: Minimize
})
