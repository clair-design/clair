import { makeIconComponent } from './utils/icon-wrapper'
import { Target } from '@clair/icons'
export const IconTarget = /*@__PURE__*/ makeIconComponent({
  name: 'CIconTarget',
  template: Target
})
