import { makeIconComponent } from './utils/icon-wrapper'
import { Refresh } from '@clair/icons'
export const IconRefresh = /*@__PURE__*/ makeIconComponent({
  name: 'CIconRefresh',
  template: Refresh
})
