import { makeIconComponent } from './utils/icon-wrapper'
import { Document } from '@clair/icons'
export const IconDocument = /*@__PURE__*/ makeIconComponent({
  name: 'CIconDocument',
  template: Document
})
