import { makeIconComponent } from './utils/icon-wrapper'
import { Database } from '@clair/icons'
export const IconDatabase = /*@__PURE__*/ makeIconComponent({
  name: 'CIconDatabase',
  template: Database
})
