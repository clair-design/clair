import { makeIconComponent } from './utils/icon-wrapper'
import { Relaxation } from '@clair/icons'
export const IconRelaxation = /*@__PURE__*/ makeIconComponent({
  name: 'CIconRelaxation',
  template: Relaxation
})
