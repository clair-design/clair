import { makeIconComponent } from './utils/icon-wrapper'
import { Replace } from '@clair/icons'
export const IconReplace = /*@__PURE__*/ makeIconComponent({
  name: 'CIconReplace',
  template: Replace
})
