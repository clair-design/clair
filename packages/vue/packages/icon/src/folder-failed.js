import { makeIconComponent } from './utils/icon-wrapper'
import { FolderFailed } from '@clair/icons'
export const IconFolderFailed = /*@__PURE__*/ makeIconComponent({
  name: 'CIconFolderFailed',
  template: FolderFailed
})
