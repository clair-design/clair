## Justify

设置水平方向的布局行为。

> 含义与 `Flexbox` 中的 `justify-content` 不同。具体表现形式参考 Props 表格。

```jsx live=true className=s-grid-example
<Row justify="end" style={{ gridTemplateColumns: "repeat(12, 30px)" }}>
  <Col>1</Col>
  <Col>2</Col>
  <Col>3</Col>
</Row>
```
