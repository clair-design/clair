## 自定义尺寸

自定义卡片尺寸，`size` 可取值: `"normal" | "small"`，默认为`"normal"`

```tsx live=true
<>
  <Card title="这是标题" actions="这是操作" size="small">
    <p>这是内容1</p>
    <p>这是内容2</p>
    <p>这是内容3</p>
  </Card>
</>
```
