## 禁用样式

通过`disabled`属性设置禁用样式

```tsx live=true className=s-container--button
<>
  <Checkbox disabled={true} defaultChecked={true}>
    默认选中
  </Checkbox>
  <Checkbox disabled={true}>未选中</Checkbox>
</>
```
