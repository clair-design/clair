# router mode: `hash` or `history`
routerMode: history

# root
rootDir: ./docs/

# dest directory
output: .site
# markdown files
documents:
  - ./docs/content/**/*.md
  - ./src/components/**/index.md

# styles
postcss:
  entry: ./docs/assets/css/main.css
  minify: true
  sourcemap: true
  plugins:
    - postcss-easy-import
    - postcss-each
    - postcss-for
    - postcss-cssnext

externals:
  - https://lib.baomitu.com/font-awesome/4.7.0/css/font-awesome.min.css

# this is an extra function
# run if `NODE_ENV` equals 'production'
libary:
  name: Clair
  input: ./src/js/main.js
  output:
    - format: umd
      file: ./dist/clair.js
    - format: es
      file: ./dist/clair.esm.js
    - format: cjs
      file: ./dist/clair.common.js
  postcss:
    extract: ./dist/clair.css
    minify: true
    sourcemap: true
    plugins:
      - postcss-easy-import
      - postcss-each
      - postcss-for
      - postcss-cssnext:
          warnForDuplicates: false
